执行仿真，选择第一个参数方案，计算方案选择“电磁暂态仿真”，计算仿真结果中，输出结果“故障零序电流RMS”的平均值
        ```'codes':```python# 选择第一个参数方案

 import time # 获取模型的最新版本 revision = cloudpss_substation_Model.revision # 选择第一个参数方案和“电磁暂态仿真”计算方案 jobs = cloudpss_substation_Model.getModelJob('电磁暂态仿真') if not jobs:    print("未找到名称为 '电磁暂态仿真' 的计算方案")    exit() job = jobs[0] config = cloudpss_substation_Model.configs[0] # 运行仿真任务 runner = revision.run(job, config) # 等待仿真完成 while not runner.status():    time.sleep(1) # 获取仿真结果的plots曲线 plots = runner.result.getPlots() # 查找“故障零序电流RMS:0”的曲线数据 fault_zero_seq_current_data = None for plot in plots:    for trace in plot['data']['traces']:        if trace['name'] == '故障零序电流RMS:0':            fault_zero_seq_current_data = trace['y']            break    if fault_zero_seq_current_data:        break # 计算平均故障零序电流RMS if fault_zero_seq_current_data:    average_fault_zero_seq_current = sum(fault_zero_seq_current_data) / len(fault_zero_seq_current_data)    print(f"故障零序电流RMS:0 的平均值为: {average_fault_zero_seq_current}") else:    print("未找到 故障零序电流RMS:0 的数据") ```