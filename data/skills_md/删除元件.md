删除元件component_new_constant_4，可以使用model.removeComponent(key),然后getAllComponents取key取所有元件,检查元件是否在元件表中，没有则已经删除
        ```'codes':```python# 获取所有元件的keyall_components = cloudpss_substation_Model.getAllComponents()
print("所有元件的key：")
for key in all_components:
 print(key)

# 检查要删除的元件是否在元件表中component_key = "component_new_constant_4"
if component_key in all_components:
 cloudpss_substation_Model.removeComponent(component_key)
 print(f"已删除元件：{component_key}")
else:
 print(f"元件{component_key}不存在")
```
'output':```python所有元件的key：
edge_OnpD4d0dk4LKedge_P0hiX4pS8nwnedge_PERyO5UQiMSUedge_PMKVfJKqinpy...
元件component_new_constant_4不存在```
        